import api from "@configs/api";
import { generateQueryString } from "@utils/methods";

//todo
//Define all the shared react query functions below check the below sample
// export const getPostsQueryFn = async () => api.get(`/posts`).then((res) => res);
